'use strict'

// function onKeywordsFilter(input) {
//     let images = gImgs
//     const filteredData = images.filter(value => {
//         const searchStr = input.toLowerCase();
//         const oneItemMatches = value.keywords.some(item => item.toLowerCase().includes(searchStr));
  
//         return oneItemMatches;
//       });
//       console.log(filteredData);
// }